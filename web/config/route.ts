export const publicRoutes = [
  "/",
  "/invite"
]

export const authRoutes = [
  "/login",
  "/login/auth",
  "/register",
  "/register/success",
  "/register/auth"
]

export const apiAuthPrefix = "/api/auth"

export const DEFAULT_REDIRECT = "/workbench"
